# cinode-client — current plan

No version is in progress. v0.1 is released; its plan is archived at
[`docs/plans/v0.1.md`](plans/v0.1.md).

The next version is listed in [`docs/roadmap.md`](roadmap.md). It starts with
a designer session (see *Starting a version* in [`CLAUDE.md`](../CLAUDE.md)),
whose handoff PR replaces this file with the version's plan. At the release,
the plan moves to `docs/plans/v<major>.<minor>.md`.
